name: Deploy to Surge

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy to Surge
        run: npx surge . ptanstack.surge.sh --token ${{ secrets.SURGE_TOKEN }}
